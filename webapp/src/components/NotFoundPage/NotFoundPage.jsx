import { React } from 'react';

// Import mui components
import {
    Box,
} from '@mui/material';

const NotFoundPage = () => {
    return (
        <Box>
            <h1>404: Page not found</h1>
        </Box>
    );
}

export default NotFoundPage;
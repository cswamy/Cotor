import { React } from 'react';

// Import mui components
import {
  Box,
} from '@mui/material';

const IssueDisplay = () => {
    return (
        <Box>
            <h1>Issue Display</h1>
        </Box>
    );
};

export default IssueDisplay;
